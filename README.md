# Vector-Error-Correction
Simulate and fit from Vector Error Correction (VECM) models for cointegrated time series using the Johansen method. The program `xvecm_rank_gap.f90` simulates a VECM, computes the Johansen eigenvalues, and estimates the cointegration rank without critical values by choosing the largest drop between successive ordered eigenvalues.
